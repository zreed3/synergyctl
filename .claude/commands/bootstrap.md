# bootstrap

Read `README.md`, `AGENTS.md`, `docs/api-coverage-matrix.md`, `fixtures/coverage/api-surfaces.json`, `LICENSE`, and `NOTICE`.

Summarize the current repo contract, note any drift, and propose only additive edits.
